<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <title>PG Buddy</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <!-- Font Awesome (for chevron-up icon) -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-yH4aZT8+x5jv3eAFYkZpBzY5+UQe9pBiC+O5kQ58+7tvO5vDHu+q3NbsfBdUJksCWNN3Nk2LBXYL3Yv6Bx1r1g==" crossorigin="anonymous" referrerpolicy="no-referrer" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/app.css"/>

  <style>
    /* Back to Top Button Styling */
    #backToTop {
      position: fixed;
      bottom: 80px;
      right: 30px;
      z-index: 9999;
      width: 48px;
      height: 48px;
      background-color: #222;
      border: none;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
      cursor: pointer;
      opacity: 1;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    #backToTop:hover {
      background-color: #444;
      transform: scale(1.1);
    }

    #backToTop i {
      color: white;
      font-size: 20px;
    }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">
  <!-- Navbar -->
  <%- include('../partials/navbar') %>

  <!-- Page Content -->
  <main class="container mt-4">
    <%- include('../partials/flash') %>
    <%- body %>
  </main>

  <!-- Footer -->
  <%- include('../partials/footer') %>

  <!-- Back to Top Button -->
  <button id="backToTop" title="Go to top">
    <i class="fa-solid fa-chevron-up"></i>
  </button>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Scroll to Top Script -->
  <script>
    document.getElementById("backToTop").addEventListener("click", function () {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
  </script>
</body>
</html>